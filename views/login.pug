<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login 
        .container-form
            h1 Welcome, please log in

         
            
            if locals.messages
                .alerts  
                    each category in Object.keys(locals.messages)
                        each error in messages[category]
                            .alert(class=`${category}`)
                                p= error

            form#form.box.box-login(method="POST" action="/login")
                .field 
                    label(for="email") Email: 
                    input(type="text" name="email" placeholder="youremail@example.com" value= email )
                .field 
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="your password" value= password )
                .field.send 
                    input.button(type="submit" value="Log in")
                .field.actions 
                    a(href="/signup") Create new account
                    a(href="/reset") Forgot password?
            
